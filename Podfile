# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-lotieAnmitionView' do
pod 'lottie-ios'
pod 'MBProgressHUD'

target 'ios-lotieAnmitionViewTests' do
    inherit! :search_paths
   
  end

  target 'ios-lotieAnmitionViewUITests' do
    inherit! :search_paths
   
  end

end
